<%= form_for @rental, :html => {:class => "form-horizontal"} do |f| %>
  <% if @rental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rental.errors.count, "error") %> prohibited this rental from being saved:</h2>

      <ul>
      <% @rental.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label class="col-sm-2 control-label">Rental name* :</label>
    <div class="col-sm-10">
      <%= f.text_field :name, placeholder: 'Enter location name...', required: true, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-primary">Update</button>
      <%= link_to 'Back', rentals_path, class:"btn btn-default" %>
    </div>
  </div>

<% end %>
